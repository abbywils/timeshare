<p id="notice"><%= notice %></p>

<div class="p-5 container">
  <h1>Countries</h1>
</div>

<div class="p-5 container">
  <table class="table .thead-dark table-hover table-responsive">
    <thead>
      <tr>
        <th>Countryname</th>
        <th>Region</th>
        <th colspan="3"></th>
      </tr>
    </thead>
  
    <tbody>
      <% @countries.each do |country| %>
        <tr>
          <td><%= country.countryname %></td>
          <td><%= country.region %></td>
          <td><%= link_to 'Show', country, class: 'btn btn-primary' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div

<%= will_paginate @countries %>

<br>








<p id="notice"><%= notice %></p>

<div class="p-5 container">
  <h1>Sites</h1>
</div>


<div class="p-5 container">
  <table class="table .thead-dark table-hover table-responsive">
    <thead>
      <tr>
        <th>Sitename</th>
        <th>Dateinscribed</th>
        <th>Longitude</th>
        <th>Latitude</th>
        <th>Area</th>
        <th>Site Type</th>
        <th>Countryname</th>
        <th>Country</th>
        <th colspan="3"></th>
      </tr>
    </thead>
  
    <tbody>
      <% @sites.each do |site| %>
        <tr>
          <td><%= site.sitename %></td>
          <td><%= site.dateinscribed %></td>
          <td><%= site.longitude %></td>
          <td><%= site.latitude %></td>
          <td><%= site.area %></td>
          <td><%= site.sitetype %></td>
          <td><%= site.countryname %></td>
          <td><%= site.country %></td>
          <td><%= link_to 'Show', site, class: 'btn btn-primary' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
</div

<%= will_paginate @countries %>

<br>